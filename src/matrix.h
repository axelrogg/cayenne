#ifndef MATRIX_H
#define MATRIX_H

#include <stdarg.h>
#include <string.h>
#include <stdio.h>

#define MAX_BUFFER_SIZE 500

void matrix_print(int **matrix, int num_rows, int num_cols);

#endif // !MATRIX_H
